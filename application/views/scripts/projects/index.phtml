<div class="body-container">
	<div class="deck-pane">
		<div id="card-deck">
			<?php if (count($this->paginator)): ?>
			<ul>
				<?php foreach ($this->paginator as $project): ?>
				<?php echo $this->projectCard($project, $this->baseUrl); ?>
				<?php endforeach; ?>
			</ul>
			<?php endif; ?>
		</div>
		<?php
		echo $this->paginationControl(
			$this->paginator,
			'Sliding',
            '/projects/partials/dot-nav.phtml'
        );
		?>
	</div>
	<a href="/projects/new/" class="pill-btn black-btn">New Project</a>
</div>
